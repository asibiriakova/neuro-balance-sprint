# Roadmap

- [x] Time-budgeting preset switcher becomes a dropdown on the planning canvas
- [x] Centralize every backend call in one services layer (`src/services`)
- [x] Mock implementation of the services layer so the app runs with no real backend
